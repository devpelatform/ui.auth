{
  "name": "@repo/auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/auth.ts",
    "./client": "./src/client.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "clean:all": "rimraf .turbo node_modules",
    "types:check": "tsc --noEmit",
    "generate": "dotenv -e ../../.env.local -- npx @better-auth/cli@latest generate --config ./src/auth.ts --output ../db/src/generated-schema.ts"
  },
  "dependencies": {
    "@repo/config": "workspace:*",
    "@repo/db": "workspace:*",
    "@repo/mail": "workspace:*",
    "better-auth": "1.3.30",
    "cookie": "^1.0.2"
  },
  "devDependencies": {
    "@better-auth/cli": "^1.3.30",
    "@repo/tsconfig": "workspace:*"
  }
}
